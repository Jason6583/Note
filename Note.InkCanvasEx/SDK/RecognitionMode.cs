namespace Note.InkCanvasEx.SDK
{
    /// <summary>识别模式</summary>
    public enum RecognitionMode
    {
        /// <summary>交互模式，边写边识别</summary>
        MODE_ICR = 1,
        /// <summary>整篇识别模式，笔划一次性传入</summary>
        MODE_ECR = 2,
    }
}
